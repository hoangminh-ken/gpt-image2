{
  "name": "gpt-image2",
  "version": "0.6.0",
  "private": true,
  "description": "GPT-Image-2 batch image generator (FastAPI + React local web app)",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green --kill-others-on-fail \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "cd backend && .venv/Scripts/python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8765",
    "dev:web": "cd frontend && npm run dev",
    "test:api": "cd backend && .venv/Scripts/python -m pytest tests/",
    "build:web": "cd frontend && npm run build",
    "install:all": "cd backend && python -m venv .venv && .venv/Scripts/pip install -r requirements.txt && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
